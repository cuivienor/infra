---
# Main task orchestration for proxmox_storage role

- name: Install storage packages
  ansible.builtin.include_tasks: install.yml
  tags: [install, storage]

- name: Configure data disks
  ansible.builtin.include_tasks: disks.yml
  tags: [disks, storage]

- name: Configure MergerFS
  ansible.builtin.include_tasks: mergerfs.yml
  tags: [mergerfs, storage]

- name: Configure SnapRAID
  ansible.builtin.include_tasks: snapraid.yml
  tags: [snapraid, storage]

- name: Set permissions
  ansible.builtin.include_tasks: permissions.yml
  tags: [permissions, storage]
