# Terraform - all root modules
terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.backup
terraform/**/*.tfstate.*.backup
terraform/**/.terraform.lock.hcl
terraform/**/terraform.tfvars
terraform/**/.terraform.tfstate.lock.info
terraform/**/tfplan
terraform/**/*.tfplan

# Ansible
*.retry
.vault_pass
ansible/.vault_pass
# Note: Encrypted vault files (backup_secrets.yml) are committed
# Only ignore if unencrypted (shouldn't happen in practice)

# Environment (secrets, not .envrc which only sets paths)
.env

# SOPS age key (private key for decryption)
terraform/.sops-key

# OS
.DS_Store

# Sensitive (plain text secrets)
*secret*
*token*
*password*

# SOPS-encrypted files are safe to commit (override *secret* pattern)
!*.sops.yaml
!**/*.sops.yaml
!secrets/

# Terraform secrets.tf files are just code, not secrets (override *secret* pattern)
!secrets.tf
!**/secrets.tf

# SOPS decrypted temp files
*.dec.yaml
*.dec.yml

# Secrets backup (local only)
.secrets-backup-*/

# BFG replacements file (contains secrets to redact)
scripts/bfg-replacements.txt

# But keep the cleanup script (doesn't contain secrets, just runs BFG)
!scripts/clean-secrets.sh

# Private journal
.private-journal/

# Temporary files
tmp/

# Editor
*.swp
*.swo
*~
.vscode/
.idea/

# MakeMKV secrets (unencrypted)
ansible/vars/makemkv_secrets.yml
!ansible/vars/makemkv_secrets.yml.example

.direnv/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Nix build outputs
result
result-*

# Claude Code local settings (personal permissions)
.claude/settings.local.json
