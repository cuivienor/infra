theme = catppuccin-mocha

# Font configuration
font-family = MonoLisa
font-size = 16
font-feature = +calt
font-feature = +liga

# Adjust spacing (macOS font rendering differences)
adjust-cell-width = -1
font-thicken = false

# Enable URL detection and make them clickable
# URLs will be clickable when holding Cmd (on macOS) or Ctrl (on Linux)
link-url = true

